name: gui
title: GPU access for isolated GUI tests
base: ubuntu@26.04
version: "0.2"
summary: GPU access without the host desktop or audio session
description: |
  Exposes GPU devices to the workshop. GUI tests start their own display and
  virtual audio servers inside the workshop.
license: MIT

plugs:
  gpu:
    interface: gpu
